<%= render(layout: "show") do %>
  <div>
    <p>This character entry does not exist. <%= link_to("Create new character entry", new_character_path(character: { name: params[:name] })) %>.</p>
  </div>
<% end %>
