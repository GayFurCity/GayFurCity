<h4>You are reporting the following artist:</h4>
<div class="page"><%= link_to(@ticket.model.name, artist_path(id: @ticket.model.id)) %></div>
<span class="date"><%= time_ago_in_words_tagged(@ticket.model.created_at) %></span>
<br><br>
<div class="dtext-container">
  <%= format_text(@ticket.model.notes) %>
</div>
