<h1>Login Reminder</h1>

<p>If you supplied an email address when signing up, <%= AdminConfig.instance.app_name %> can email you your login information. Password details will not be provided and will not be changed.</p>

<p>If you didn't supply a valid email address, you are out of luck.</p>

<%= custom_form_for(:user, url: users_login_reminder_path) do |f| %>
  <%= f.input(:email) %>
  <%= f.submit("Submit") %>
<% end %>

<%= render("sessions/secondary_links") %>

<% content_for(:page_title) do %>
  Login Reminder
<% end %>
