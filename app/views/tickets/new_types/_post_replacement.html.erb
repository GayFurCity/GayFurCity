You are reporting the following replacement:
<div class="replacement-embedded">
  <%= render(partial: "posts/replacements/card", locals: { post_replacement: @ticket.model, expanded: true, actions: false }) %>
</div>
